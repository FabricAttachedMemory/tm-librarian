---------------------------------------------------------------------------
Packaging tm-librarian for L4TM
---------------------------------------------------------------------------
- login to "hlinux-build.us.rdlabs.hpecorp.net"
- git clone git@github.hpe.com:hpelinux/tm-librarian.git
- cd tm-librarian
- ensure you are on a development branch
- edit debian/changelog file, create new entry and increment version
- commit change, push to remote and merge into master branch
- ensure you are on the master branch "git checkout master"
- create a git tag for the commit you want to package (ex: git tag -a v0.3-1+l4tm1)
- push the tag to github master branch "git push origin master --follow-tags"
- create the debian package "dpkg-buildpackage -tc"
- packaged files will be located in the parent directory
- check that the package passes all tests "dput -o l4tm *.changes"
- submit the package to the l4tm queue "dput l4tm *.changes"
